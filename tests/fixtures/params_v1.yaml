openapi: "3.0.3"
info:
  title: Parameter Test API
  version: "1.0.0"
paths:
  /items:
    get:
      summary: List items
      operationId: listItems
      parameters:
        - name: filter
          in: query
          required: true
          schema:
            type: string
        - name: category
          in: query
          required: false
          schema:
            type: string
            enum: [a, b, c]
        - name: sort
          in: header
          required: false
          schema:
            type: string
      responses:
        "200":
          description: Items
